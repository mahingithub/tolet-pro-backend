import { NetworkFrequencyStrategy } from './index';
import { WebLogCache } from '../log-cache-web';
export declare class LowFrequencySmallChunkStrategy extends NetworkFrequencyStrategy {
    constructor(cache: WebLogCache);
}
